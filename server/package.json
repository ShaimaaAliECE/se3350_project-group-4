{
  "dependencies": {
    "json-server": "^0.17.0"
  },
  "scripts": {
    "server": "node server.js",
    "start": "json-server --watch db.json --port 3001"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
